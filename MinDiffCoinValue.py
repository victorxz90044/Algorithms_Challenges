def MinDiffCoin(arr):
# sort
    sorted = arr.sort()
#get the difference in the adjucant values
    #xdiff =  for n in range(1,len(arr))]
    for(n in arr):
        [arr[n]-arr[n-1]
     #if current is smaller then save. save the new one
       
#return the min difference


